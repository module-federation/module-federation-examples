{
  "private": true,
  "scripts": {
    "start": "npx lerna run --scope @vue-cli/* --parallel start",
    "build": "npx lerna run --scope @vue-cli/* build",
    "serve": "npx lerna run --scope @vue-cli/* --parallel serve",
    "clean": "npx lerna run --scope @vue-cli/* --parallel clean"
  },
  "devDependencies": {
    "lerna": "3.22.1"
  }
}
