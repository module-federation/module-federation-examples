{
  "name": "vue-cli",
  "private": true,
  "workspaces": [
    "./consumer",
    "./core",
    "./other"
  ],
  "version": "0.0.0",
  "scripts": {
    "start": "pnpm --filter --scope @vue-cli/* --parallel start",
    "build": "pnpm --filter --scope @vue-cli/* build",
    "serve": "pnpm --filter --scope @vue-cli/* --parallel serve",
    "clean": "pnpm --filter --scope @vue-cli/* --parallel clean"
  },
  "devDependencies": {}
}
