{
  "name": "vue-cli",
  "scripts": {
    "start": "lerna run --scope=vue-cli* --parallel start",
    "build": "lerna run --scope=vue-cli* --parallel build",
    "serve": "lerna run --scope=vue-cli* --parallel serve",
    "clean": "lerna run --scope=vue-cli* --parallel clean"
  },
  "devDependencies": {
    "lerna": "3.22.1"
  }
}
