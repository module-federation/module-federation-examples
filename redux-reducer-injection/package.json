{
  "name": "redux-reducer-injection",
  "scripts": {
    "start": "npx concurrently \"npx lerna run --scope=redux-reducer-injection_app* --parallel start\" \"yarn serve\"",
    "build": "npx lerna run --scope=redux-reducer-injection_app* build",
    "serve": "npx lerna run --scope=redux-reducer-injection_app* --parallel serve",
    "clean": "npx lerna run --scope=redux-reducer-injection_app* --parallel clean",
    "e2e:ci": "yarn start & npx wait-on http-get://localhost:3001/ && npx cypress run --config-file ../cypress/config/cypress.config.ts --config '{\"supportFile\": \"../cypress/support/e2e.ts\"}' --spec \"./e2e/*.cy.ts\" --browser=chrome"
  }
}
