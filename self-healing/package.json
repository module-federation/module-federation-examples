{
  "name": "self-healing",
  "scripts": {
    "start": "npx lerna run --scope=self-healing_app* --parallel start",
    "build": "npx lerna run --scope=self-healing_app* build",
    "serve": "npx lerna run --scope=self-healing_app* --parallel serve",
    "clean": "npx lerna run --scope=self-healing_app* --parallel clean",
    "e2e:ci": "yarn start & npx wait-on http-get://localhost:3001/ && npx cypress run --config-file ../cypress/config/cypress.config.ts --config '{\"supportFile\": \"../cypress/support/e2e.ts\"}' --spec \"./e2e/tests/*.cy.ts\" --browser=chrome"
  }
}
