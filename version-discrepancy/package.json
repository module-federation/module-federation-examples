{
  "name": "version-discrepancy",
  "private": true,
  "workspaces": [
    "./app1",
    "./app2"
  ],
  "version": "0.0.0",
  "scripts": {
    "start": "pnpm --filter version-discrepancy_app* --parallel start",
    "build": "pnpm --filter version-discrepancy_app* build",
    "serve": "pnpm --filter version-discrepancy_app* --parallel serve",
    "clean": "pnpm --filter version-discrepancy_app* --parallel clean",
    "e2e:ci": "pnpm start & wait-on http-get://localhost:3001/ && npx cypress run --config-file ../cypress/config/cypress.config.ts --config '{\"supportFile\": \"../cypress/support/e2e.ts\"}' --spec \"./e2e/tests/*.cy.ts\" --browser=chrome"
  },
  "devDependencies": {
    "wait-on": "7.0.1"
  }
}
