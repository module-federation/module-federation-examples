{
  "name": "version-discrepancy",
  "scripts": {
    "start": "lerna run --scope=version-discrepancy_app* --parallel start",
    "build": "lerna run --scope=version-discrepancy_app* --parallel build",
    "serve": "lerna run --scope=version-discrepancy_app* --parallel serve",
    "clean": "lerna run --scope=version-discrepancy_app* --parallel clean"
  },
  "devDependencies": {
    "lerna": "3.22.1"
  }
}
