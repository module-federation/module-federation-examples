{
  "name": "css-isolation",
  "workspaces": {
    "packages": [
      "app1",
      "app2"
    ]
  },
  "scripts": {
    "start": "lerna run --scope=css-isolation_app* --parallel start",
    "build": "lerna run --scope=css-isolation_app* --parallel build",
    "serve": "lerna run --scope=css-isolation_app* --parallel serve",
    "clean": "lerna run --scope=css-isolation_app* --parallel clean"
  },
  "devDependencies": {
    "lerna": "3.22.1"
  }
}
