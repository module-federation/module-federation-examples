{
  "private": true,
  "scripts": {
    "start": "lerna run --scope @shared-routes2/* --parallel start",
    "build": "lerna run --scope @shared-routes2/* build",
    "serve": "lerna run --scope @shared-routes2/* --parallel serve",
    "clean": "lerna run --scope @shared-routes2/* --parallel clean"
  },
  "devDependencies": {
    "lerna": "3.22.1"
  }
}
