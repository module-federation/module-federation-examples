{
  "$schema": "http://json-schema.org/schema",
  "type": "object",
  "cli": "nx",
  "title": "Native Federation serve executor",
  "version": 2,
  "properties": {
    "buildTarget": {
      "type": "string",
      "description": "Command to run Native Federation build executor"
    },
    "port": {
      "type": "number",
      "description": "Port to serve on"
    },
    "watch": {
      "type": "boolean",
      "description": "Watches for changes and rebuilds application"
    }
  }
}
