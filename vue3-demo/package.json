{
  "name": "vue3-demo",
  "scripts": {
    "start": "lerna run --scope=vue3-demo* --parallel start",
    "build": "lerna run --scope=vue3-demo* --parallel build",
    "serve": "lerna run --scope=vue3-demo* --parallel serve",
    "clean": "lerna run --scope=vue3-demo* --parallel clean"
  },
  "devDependencies": {
    "lerna": "3.22.1"
  }
}
