{
  "name": "vue3-demo",
  "version": "0.0.0",
  "scripts": {
    "start": "pnpm --filter vue3-demo_* --parallel start",
    "build": "pnpm --filter vue3-demo_* --parallel build",
    "serve": "pnpm --filter vue3-demo_* --parallel serve",
    "clean": "pnpm --filter vue3-demo_* --parallel clean",
    "e2e:ci": "pnpm start & wait-on http-get://localhost:3001/ && pnpm cypress run --config-file ../cypress-e2e/config/cypress.config.ts --config '{\"supportFile\": \"../cypress-e2e/support/e2e.ts\"}' --spec \"./e2e/tests/*.cy.ts\" --browser=chrome"
  },
  "devDependencies": {
    "wait-on": "7.2.0"
  }
}
