{
  "name": "shared-context",
  "scripts": {
    "start": "lerna run --scope=shared-context_app* --parallel start",
    "build": "lerna run --scope=shared-context_app* --parallel build",
    "serve": "lerna run --scope=shared-context_app* --parallel serve",
    "clean": "lerna run --scope=shared-context_app* --parallel clean"
  },
  "devDependencies": {
    "lerna": "3.22.1",
    "webpack-cli": "^5.0.1"
  }
}
