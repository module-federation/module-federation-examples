{
  "name": "shared-context",
  "scripts": {
    "start": "npx lerna run --scope=shared-context_* --parallel start",
    "build": "npx lerna run --scope=shared-context_* build",
    "serve": "npx lerna run --scope=shared-context_* --parallel serve",
    "clean": "npx lerna run --scope=shared-context_* --parallel clean",
    "e2e:ci": "yarn start & npx wait-on http-get://localhost:3001/ && npx cypress run --config-file ../cypress/config/cypress.config.ts --config '{\"supportFile\": \"../cypress/support/e2e.ts\"}' --spec \"./e2e/*.cy.ts\" --browser=chrome"
  },
  "devDependencies": {
    "webpack-cli": "^5.0.1"
  }
}
