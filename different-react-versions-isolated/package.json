{
  "name": "different-react-versions-isolated",
  "version": "0.0.0",
  "scripts": {
    "start": "npx lerna run --scope=different-react-versions-isolated_app* --parallel start",
    "build": "npx lerna run --scope=different-react-versions-isolated_app* build",
    "serve": "npx lerna run --scope=different-react-versions-isolated_app* --parallel serve",
    "clean": "npx lerna run --scope=different-react-versions-isolated_app* --parallel clean",
    "e2e:ci": "yarn start & npx wait-on http-get://localhost:3001/ && npx cypress run --config-file ../cypress/config/cypress.config.ts --config '{\"supportFile\": \"../cypress/support/e2e.ts\"}' --spec \"./e2e/*.cy.ts\" --browser=chrome"
  }
}
