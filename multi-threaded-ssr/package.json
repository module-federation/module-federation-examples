{
  "name": "multi-threaded-ssr",
  "scripts": {
    "start": "yarn build && yarn serve",
    "build": "lerna run --scope=multi-threaded-ssr* --parallel build",
    "clean": "lerna run --scope=multi-threaded-ssr* --parallel clean"
  },
  "devDependencies": {
    "lerna": "3.22.1"
  },
  "license": "CC BY-NC-ND 4.0"
}
