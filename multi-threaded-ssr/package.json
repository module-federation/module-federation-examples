{
  "private": true,
  "scripts": {
    "start": "yarn build && yarn serve",
    "build": "lerna run --scope @multi-threaded-ssr/icarus-2 build && lerna run --scope @multi-threaded-ssr/icarus-1 build",
    "serve": "lerna run --scope @multi-threaded-ssr/* --parallel serve",
    "clean": "lerna run --scope @multi-threaded-ssr/* --parallel clean"
  }
}
