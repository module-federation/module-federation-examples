{
  "private": true,
  "scripts": {
    "start": "yarn build && yarn serve",
    "build": "npx lerna run build --parallel --scope @multi-threaded-ssr/icarus-2 --scope @multi-threaded-ssr/icarus-1",
    "clean": "npx lerna run --scope @multi-threaded-ssr/* --parallel clean"
  },
  "license": "CC BY-NC-ND 4.0"
}
