{
  "private": true,
  "scripts": {
    "start": "npx lerna run --scope @streamed-federation/* --parallel start",
    "build": "npx lerna run --scope @streamed-federation/* --parallel build",
    "serve": "npx lerna run --scope @streamed-federation/* --parallel serve",
    "clean": "npx lerna run --scope @streamed-federation/* --parallel clean",
    "setup": "npx lerna run --scope @streamed-federation/* --parallel setup",
    "deploy:local": "npx lerna run --scope @streamed-federation/* --parallel deploy:local",
    "deploy": "npx lerna run --scope @streamed-federation/* --parallel deploy",
    "setup:local": "npx lerna run --scope @streamed-federation/* --parallel setup:local",
    "start:s3": "s3rver -d ./.s3rver"
  },
  "license": "CC BY-NC-ND 4.0",
  "dependencies": {
    "s3rver": "^3.5.0",
    "serverless": "^1.69.0",
    "wsrun": "^5.2.0"
  }
}
