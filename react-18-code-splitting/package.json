{
  "name": "react-18-code-splitting",
  "private": true,
  "version": "0.0.0",
  "workspaces": [
    "app1",
    "app2"
  ],
  "scripts": {
    "start": "pnpm --filter react-18-code-splitting_* --parallel start",
    "build": "pnpm --filter react-18-code-splitting_* build",
    "serve": "pnpm --filter react-18-code-splitting_* --parallel serve",
    "clean": "pnpm --filter react-18-code-splitting_* --parallel clean",
    "e2e:ci": "yarn start & wait-on http-get://localhost:3000/ && npx cypress run --config-file ../cypress/config/cypress.config.ts --config '{\"supportFile\": \"../cypress/support/e2e.ts\"}' --spec \"./e2e/*.cy.ts\" --browser=chrome"
  },
  "devDependencies": {
    "lerna": "3.22.1"
  }
}
