import styles from './index.module.less';

const LandingPage = (props: any): JSX.Element => {
  // const [count, setCount] = useState(0);
  console.log(props);
  return <div className={styles.libRow}>landing-page react 17</div>;
};

export default LandingPage;
