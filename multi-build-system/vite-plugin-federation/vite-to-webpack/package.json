{
  "private": true,
  "workspaces": {
    "packages": [
      "vite-host",
      "webpack-remote"
    ]
  },
  "scripts": {
    "start": "lerna run --parallel start",
    "build": "lerna run build",
    "serve": "lerna run --parallel serve",
    "clean": "lerna run --parallel clean",
    "cypress:ci": "concurrently \"cd webpack-remote && yarn start\" \"wait-on http:://localhost:3000\" \"cd  vite-host && yarn start\" \"wait-on http:://localhost:5000\" && cypress open --e2e --config-file cypress.config.js --browser=chrome"
  },
  "devDependencies": {
    "lerna": "3.22.1"
  },
  "dependencies": {
    "concurrently": "^7.6.0",
    "cypress": "^12.3.0",
    "typescript": "^4.9.4"
  }
}
