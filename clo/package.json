{
  "name": "clo",
  "version": "0.0.1",
  "description": "Create React app Running with rsbuild",
  "workspaces": [
    "./host",
    "./remote"
  ],
  "scripts": {
    "start": "pnpm --filter clo_* start",
    "build": "pnpm --filter clo_* build",
    "preview": "pnpm run build && concurrently \"pnpm --filter clo_host preview\" \"pnpm --filter clo_remote preview\"",
    "test:e2e": "pnpm run build && pnpm exec playwright test",
    "e2e:ci": "pnpm run build && pnpm exec playwright test --reporter=list",
    "legacy:e2e:ci": "echo \"No legacy e2e tests\""
  },
  "devDependencies": {
    "wait-on": "7.2.0",
    "concurrently": "8.2.2",
    "forever": "4.0.3",
    "@playwright/test": "1.58.1",
    "playwright": "1.58.1"
  }
}
