{
  "name": "apollo-client",
  "version": "0.0.0",
  "scripts": {
    "start": "pnpm --filter --parallel start",
    "serve": "pnpm --filter --parallel serve",
    "build": "pnpm --filter build",
    "e2e:ci": "pnpm --filter apollo-client-app2 run build && pnpm --filter apollo-client-app1 run build"
  },
  "devDependencies": {}
}
