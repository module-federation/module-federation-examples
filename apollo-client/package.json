{
  "name": "apollo-client",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "app1",
    "app2"
  ],
  "scripts": {
    "start": "lerna run --parallel start",
    "serve": "lerna run --parallel serve",
    "build": "lerna run build"
  },
  "devDependencies": {}
}
