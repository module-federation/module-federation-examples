{
  "name": "@apollo-client",
  "private": true,
  "version": "0.0.0",
  "workspaces": [
    "app1",
    "app2"
  ],
  "scripts": {
    "start": "pnpm --filter --parallel start",
    "serve": "pnpm --filter --parallel serve",
    "build": "pnpm --filter build"
  },
  "devDependencies": {
    "lerna": "3.22.1"
  }
}
