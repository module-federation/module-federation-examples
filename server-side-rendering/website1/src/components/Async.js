import React from "react";
// eslint-disable-next-line
export default () => (
  <div>
    <h2 onClick={() => alert("Interactive chunk from website1")}>
      This is loadable component from website1
    </h2>
  </div>
);
