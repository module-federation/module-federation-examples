import React from "react";
// eslint-disable-next-line
export default () => (
  <div>
    <h3 onClick={() => alert("Interactive chunk from website2")}>
      This is loadable component from website2
    </h3>
  </div>
);
