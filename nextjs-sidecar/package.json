{
  "private": true,
  "scripts": {
    "start": "lerna run --scope @nextjs-sidecar/* --parallel start",
    "build": "lerna run --scope @nextjs-sidecar/* build",
    "serve": "lerna run --scope @nextjs-sidecar/* --parallel serve",
    "clean": "lerna run --scope @nextjs-sidecar/* --parallel clean"
  },
  "devDependencies": {
    "lerna": "^3.22.1"
  }
}
