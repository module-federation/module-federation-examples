{
  "name": "vue3-cli-demo",
  "description": "Vue 3 using vue-cli OR rsbuild",
  "private": true,
  "scripts": {
    "test:e2e": "pnpm exec playwright test",
    "test:e2e:ui": "pnpm exec playwright test --ui",
    "e2e:ci": "pnpm exec playwright test --reporter=line"
  },
  "devDependencies": {
    "@playwright/test": "^1.54.2"
  }
}
