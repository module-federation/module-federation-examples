{
  "name": "cra",
  "private": true,
  "version": "0.0.1",
    "workspaces": [
      "./host",
      "./remote"
    ],
  "scripts": {
    "start": "lerna run --scope=cra_* start",
    "stop": "lerna run --scope=cra_* stop",
    "build": "lerna run --scope=cra_* build'",
    "serve": "lerna run --scope=cra_* serve'",
    "clean": "lerna run --scope=cra_* clean'",
    "e2e:ci": "yarn start && npx cypress run --config-file ../cypress/config/cypress.config.js --config '{\\\"supportFile\\\": \\\"../cypress/support/e2e.js\\\"}' --spec \\\"./e2e/*.cy.js\\\" --browser=chrome && yarn stop"
  },
  "devDependencies": {
    "concurrently": "7.6.0",
    "forever": "4.0.3"
  }
}
