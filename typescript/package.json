{
  "private": true,
  "scripts": {
    "start": "npx lerna run --scope @typescript/* --parallel start",
    "build": "npx lerna run --scope @typescript/* build",
    "serve": "npx lerna run --scope @typescript/* --parallel serve",
    "clean": "npx lerna run --scope @typescript/* --parallel clean"
  }
}
