{
  "name": "typescript",
  "scripts": {
    "start": "lerna run --scope=typescript_app* --parallel start",
    "build": "lerna run --scope=typescript_app* --parallel build",
    "serve": "lerna run --scope=typescript_app* --parallel serve",
    "clean": "lerna run --scope=typescript_app* --parallel clean"
  },
  "devDependencies": {
    "lerna": "3.22.1"
  }
}
