{
  "name": "vue3-demo-federation-with-vite",
  "scripts": {
    "build": "npx pnpm --parallel --filter \"./**\" build",
    "serve": "npx pnpm --parallel --filter \"./**\"  serve",
    "e2e:ci": "npx pnpm build && pnpm serve & npx wait-on http-get://localhost:5000/ && npx cypress run --config-file ../cypress/config/cypress.config.ts --config '{\"supportFile\": \"../cypress/support/e2e.ts\"}' --spec \"./e2e/tests/runAll.cy.ts\" --browser=chrome"
  }
}
