{
  "name": "different-react-versions",
  "workspaces": {
    "packages": [
      "app1",
      "app2"
    ]
  },
  "scripts": {
    "start": "lerna run --scope=different-react-versions_app* --parallel start",
    "build": "lerna run --scope=different-react-versions_app* --parallel build",
    "serve": "lerna run --scope=different-react-versions_app* --parallel serve",
    "clean": "lerna run --scope=different-react-versions_app* --parallel clean"
  },
  "devDependencies": {
    "lerna": "3.22.1"
  }
}
