{
  "name": "013-styled-components",
  "version": "0.0.1",
  "private": true,
  "workspaces": [
    "app1",
    "app2"
  ],
  "scripts": {
    "start": "pnpm --filter --parallel start",
    "serve": "pnpm --filter --parallel serve",
    "build": "pnpm --filter build"
  },
  "devDependencies": {
    "lerna": "3.22.1"
  }
}
