{
  "name": "typescript-project-references",
  "scripts": {
    "start": "npx lerna run --scope=typescript-project-references_app* --parallel start",
    "build": "npx lerna run --scope=typescript-project-references_app* build",
    "serve": "npx lerna run --scope=typescript-project-references_app* --parallel serve",
    "clean": "npx lerna run --scope=typescript-project-references_app* --parallel clean",
    "e2e:ci": "yarn start & wait-on http-get://localhost:3001/ && npx cypress run --config-file ../cypress/config/cypress.config.ts --config '{\"supportFile\": \"../cypress/support/e2e.ts\"}' --spec \"./e2e/tests/*.cy.ts\" --browser=chrome"
  },
  "devDependencies": {
    "lerna": "3.22.1",
    "wait-on": "7.0.1"
  }
}
