{
  "name": "typescript-project-references",
  "scripts": {
    "start": "lerna run --scope=typescript-project-references_app* --parallel start",
    "build": "lerna run --scope=typescript-project-references_app* --parallel build",
    "serve": "lerna run --scope=typescript-project-references_app* --parallel serve",
    "clean": "lerna run --scope=typescript-project-references_app* --parallel clean"
  },
  "devDependencies": {
    "lerna": "3.22.1"
  }
}
