{
  "name": "angular-universal-ssr",
  "scripts": {
    "start": "yarn build && yarn serve",
    "build": "npx lerna run --scope=angular-universal-ssr_* build:federation",
    "serve": "npx lerna run --scope=angular-universal-ssr_* --parallel serve:federation",
    "clean": "npx lerna run --scope=angular-universal-ssr_* --parallel clean"
  }
}
