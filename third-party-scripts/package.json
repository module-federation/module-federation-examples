{
  "name": "third-party-scripts",
  "scripts": {
    "start": "lerna run --scope=third-party-scripts_app* --parallel start",
    "build": "lerna run --scope=third-party-scripts_app* --parallel build",
    "serve": "lerna run --scope=third-party-scripts_app* --parallel serve",
    "clean": "lerna run --scope=third-party-scripts_app* --parallel clean"
  },
  "devDependencies": {
    "lerna": "3.22.1"
  }
}
