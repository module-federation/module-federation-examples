{
  "name": "nested",
  "scripts": {
    "start": "lerna run --scope=nested_app* --parallel start",
    "build": "lerna run --scope=nested_app* --parallel build",
    "serve": "lerna run --scope=nested_app* --parallel serve",
    "clean": "lerna run --scope=nested_app* --parallel clean"
  },
  "devDependencies": {
    "lerna": "3.22.1"
  }
}
