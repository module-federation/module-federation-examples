import { test } from '@playwright/test';

test.describe('Legacy runAll placeholder', () => {
  test('Playwright runs individual specs automatically', async () => {
    // Maintains backwards compatibility with the former entry point.
  });
});
