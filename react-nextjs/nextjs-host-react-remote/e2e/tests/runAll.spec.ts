import { test } from '@playwright/test';

test.describe('Legacy runAll placeholder', () => {
  test('Playwright discovers individual specs automatically', async () => {
    // This spec exists to preserve the prior file layout used by Cypress runAll imports.
  });
});
