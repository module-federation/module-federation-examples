{
  "private": true,
  "scripts": {
    "start": "lerna run --scope @dashboard/* --parallel start",
    "build": "lerna run --scope @dashboard/* build",
    "serve": "lerna run --scope @dashboard/* --parallel serve",
    "clean": "lerna run --scope @dashboard/* --parallel clean"
  }
}
