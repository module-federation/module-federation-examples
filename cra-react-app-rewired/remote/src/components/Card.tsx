import React from 'react';

export const Card = () => (
    <div style={{border: '2px dotted red', padding: 20}}>
        This is a component from the remote application
    </div>
);

export default Card;
