{
  "private": true,
  "workspaces": {
    "packages": [
      "host",
      "remote"
    ]
  },
  "scripts": {
    "start": "npx concurrently 'cd ./host && yarn start' 'cd ./remote && yarn start'",
    "build": "npx concurrently 'cd ./host && yarn build' 'cd ./remote && yarn build'",
    "test": "npx concurrently 'cd ./host && yarn serve' 'cd ./remote && yarn serve'"
  }
}
