{
  "private": true,
  "scripts": {
    "start": "lerna run --scope @bi-directional/* --parallel start",
    "build": "lerna run --scope @bi-directional/* build",
    "serve": "lerna run --scope @bi-directional/* --parallel serve"
  }
}
