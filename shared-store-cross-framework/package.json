{
  "name": "shared-store-cross-framework",
  "scripts": {
    "start": "lerna run --scope=shared-store-cross-framework* --parallel start",
    "build": "lerna run --scope=shared-store-cross-framework* --parallel build",
    "serve": "lerna run --scope=shared-store-cross-framework* --parallel serve",
    "clean": "lerna run --scope=shared-store-cross-framework* --parallel clean"
  },
  "devDependencies": {
    "lerna": "3.22.1"
  }
}
